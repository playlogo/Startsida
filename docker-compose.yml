name: startsida

services:
    server:
        image: playlogo/startsida
        restart: "unless-stopped"
        build: .
        volumes:
            - ./data:/app/data
        ports:
            - "8000:8000"
        network_mode: "host"
