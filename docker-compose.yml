name: startsida

services:
    server:
        image: playlogo/startsida
        restart: "unless-stopped"
        user: "1000"
        build: .
        volumes:
            - ./data:/app/data:rw
            - ./cache:/app/cache:rw
        ports:
            - "8000:8000"
        network_mode: "host"
